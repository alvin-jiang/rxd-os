
简化：
buffer.c - bread/bwrite
bitmap.c
    sector_t alloc_block(dev_t dev)
    void free_block(dev_t dev, sector_t block)
    struct inode * alloc_inode(dev_t dev)
    void free_inode(dev_t dev, struct inode *inode)
inode.c
    注意：inode 的创建在 bitmap.c 里
    struct inode * iget(dev_t dev, unsigned long num) 从inode_table读/read_inode
    void iput(struct inode *inode) 减引用/write_inode
    void read_inode(struct inode *inode) 设置好 inode 的设备号、inode号
    void write_inode(struct inode *inode) 设置好 inode 的设备号、inode号
super.c
    ...
